@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Confirm email";
}

<h1>@Model.StatusMessage</h1>

@if (Model.WelcomeMessage)
{
    <p>You can <a asp-area="Identity" asp-page="/Account/Login">log in</a> now.</p>
}